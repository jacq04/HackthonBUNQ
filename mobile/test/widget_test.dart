// Smoke-test placeholder. Full widget tests would require mocking the
// Supabase client + Dio, which is out of scope for the hackathon.

import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder', () {
    expect(true, isTrue);
  });
}
